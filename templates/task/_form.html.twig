<div class="container">
  <div class="m-auto w-50">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <br />
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>

    {{ form_end(form) }}
    <br />
    <a href="{{ path('task_index') }}"><button class="btn btn-secondary">Cancel</button></a>
  </div>
</div>
